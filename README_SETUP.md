# amir0l0 GitHub Profile

Push this folder to `https://github.com/amir0l0/amir0l0`.

Social links use local SVG icons under `assets/social/`, so GitHub does not need to load social-media badge images from an external badge service.

After pushing, run the GitHub Actions in `.github/workflows/` to generate the profile summary cards and contribution snake.
